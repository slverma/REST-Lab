# This workflow has been superseded by release.yml.
#
# Previously: triggered on manual tag pushes (v*.*.*) to run vsce publish.
# Now: semantic-release (release.yml) handles versioning, tagging, GitHub
# Release creation, and vsce publish automatically after every merge to main.
#
# This file is kept as a historical record and has no active triggers.
# It can be deleted once the team is comfortable with the new workflow.
